apiVersion: v1
kind: Secret
metadata:
  name: agent-secrets
type: Opaque
stringData:
  # PostgreSQL credentials (default for in-cluster deployment)
  POSTGRES_USER: "postgres"
  POSTGRES_PASSWORD: "postgres"

  # SSO / OIDC Authentication
  SSO_ISSUER_URL: ""
  SSO_CLIENT_ID: ""
  SSO_CLIENT_SECRET: ""

  # Langfuse (optional - external service)
  LANGFUSE_PUBLIC_KEY: ""
  LANGFUSE_SECRET_KEY: ""
  LANGFUSE_BASE_URL: ""

  # Google Vertex AI (optional)
  GOOGLE_APPLICATION_CREDENTIALS_CONTENT: ""

  # vLLM / OpenAI-compatible (optional)
  VLLM_BASE_URL: ""
  VLLM_API_KEY: ""

  # Shared secret between agent pod and eval runner — generate with: openssl rand -hex 32
  EVAL_INTERNAL_TOKEN: ""
