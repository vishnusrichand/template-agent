apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Shared resources — included by all overlays (kind, openshift, etc.)
resources:
  - service.yaml
  - networkpolicy.yaml
  - configmap.yaml

labels:
  - pairs:
      app: eval-runner
      component: eval-runner
